# MEMORY

## Stable Facts

- This is a small Python project in early setup stage.
- Current executable module is `main.py`.
- Existing visible behavior: `print_hi(name)` prints `Hi, {name}`.
- Harness should stay simple and fast for local iteration.

## Environment Constraints

- Default local execution is `python ...`.
- Virtual environment folder exists: `venv/`.
- Keep secrets out of source files.

## Core Goal

- Maintain a lightweight Codex harness so edits are guided by
  project context and verified with executable checks.
